<% if book.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% .errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>


<% if user.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% .errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>